services:
  - type: web
    name: mini-crm
    runtime: python
    pythonVersion: "3.11.6"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
